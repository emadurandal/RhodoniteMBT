///|
/// Re-export `emadurandal/WebGPU/webgpu_objects` for JS / external `emadurandal/WebGPU` API.
pub using @w {
  type GPURenderPipeline,
  type GPUBuffer,
  type GPUCanvasContext,
  type GPUCommandBuffer,
  type GPUCommandEncoder,
  type GPUDevice,
  type GPURenderPassEncoder,
  type GPUTexture,
  type GPUTextureView,
  type GPUQueue,
  type GPUShaderModule,
  type WebGPUCanvasContext,
  type WebGPUDevice,
  type WebGPUQueue,
  device_createBuffer,
  device_createCommandEncoder,
  device_createRenderPipeline,
  device_createShaderModule,
  device_getQueue,
  encoder_beginRenderPass,
  encoder_finish,
  queue_submit,
  renderPass_draw,
  renderPass_end,
  renderPass_setPipeline,
  texture_createView,
}
